<h1>Offers#index</h1>
<p>Find me in app/views/offers/index.html.erb</p>
<p>----------------------------------------------------</p>
<%= link_to 'Logout', destroy_user_session_path, method: :delete %> <br>
<%= link_to 'Login',  new_user_session_path %> <br>

<%= link_to 'Lista de Produtos', produtos_path %>

<% if @offers.empty? %>
  <h4>Você nao tem nenhuma offer criada!</h4>
<% else %>
  <h4>Suas ofertas!</h4>
  <% @offers.each do |offer| %>
    <p> - <%= link_to offer.products.first.name, offer_path(offer.id) %> <%= link_to 'Editar oferta', edit_offer_path(offer.id)  %> <%#= link_to 'Deletar oferta', offer_path(offer.id), method: :delete, data: { confirm: 'Are you sure?' }  %> </p>
  <% end %>
<% end %>

